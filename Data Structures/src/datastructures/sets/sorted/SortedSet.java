package datastructures.sets.sorted;

import datastructures.SortedCollection;
import datastructures.sets.AbstractSet;

public interface SortedSet<T> extends AbstractSet<T>, SortedCollection<T> {
	
}
